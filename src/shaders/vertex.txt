#version 330 core

layout (location=0) in vec2 vertexPos;

out vec3 color;

uniform vec2 position;

void main()
{
	vec2 pos = (vertexPos + position) / vec2(320.0, 240.0);
	gl_Position = vec4(pos, 0.0, 1.0);
	color = vec3(1.0, 1.0, 1.0);
}